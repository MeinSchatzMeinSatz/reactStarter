import React from "react";

const react = () => {
    let counter = 0;
    return (
        <div>
            <div>{counter}</div>
            <button>클릭!</button>
        </div>
    );
};

export default Increase;
